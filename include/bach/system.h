#ifndef BACH_SYSTEM_H
#define BACH_SYSTEM_H

#define NULL ((void *)0) /* NULL is usually in stdio, so put it here */

#define ARRAY_SIZE(arr) (sizeof(arr) / sizeof((arr)[0]))

extern u32 vectors[];

#endif
